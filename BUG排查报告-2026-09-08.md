# CoAgent Hub Bug 排查报告

> 版本 v0.12.0 ｜ 排查日期 2026-09-08 ｜ 排查人：琉璃 👑
> 方法：静态代码审查（全量 20 个源文件）+ 端到端冒烟（107 项，全绿）+ **10 项实证验证脚本**（`.probe/verify-bugs.mjs`，隔离临时数据目录）

## 结论速览

| 严重度 | 数量 | 说明 |
|--------|------|------|
| **P0** | 3 | 可导致账号全丢 / 服务被完全接管，建议立即修 |
| **P1** | 8 | 功能错误或可被利用的健壮性缺陷 |
| **P2** | 7 | 体验、性能、代码健壮性 |

冒烟测试 107 项全绿 —— **但一个 bug 都没抓到**。原因是它只覆盖"正常输入 + 已声明的错误输入"，
对类型混淆、权限粒度、状态机越界、安全头缺失这四类完全没覆盖。

---

## P0 —— 必须立即修复

### P0-1 ｜ 存储型 XSS：文件附件 MIME 可伪造，可窃取管理员 token

**实证确认** ✅ 完整攻击链已跑通

| 项 | 内容 |
|----|------|
| 位置 | `src/files.js:96`、`src/server.js:400-411` |
| 触发 | 任意持有 `context:write` 的用户（**新建 agent 默认就有**）上传文件 |

攻击链：

```
攻击者在自己的机器上（普通 agent 权限）
  ↓ POST /files，Content-Type: text/html，body 为 <script>...</script>
  ↓ files.js:96 直接把请求头的 MIME 存进元数据，无任何白名单
受害者（管理员）在面板打开文件
  ↓ GET /files/:id → server.js:402 用 meta.mimeType 作为响应 Content-Type
  ↓ 且 Content-Disposition: inline（不是 attachment！）
  ↓ 浏览器按 text/html 渲染 → 脚本执行
  ↓ 读取 sessionStorage 的 token（panel.html:592）→ 攻击者完全接管账号
```

实证输出：

```
上传 200，回显 Content-Type=text/html，Disposition=inline，nosniff=无
```

**加重因素**：全站未设置 `X-Content-Type-Options: nosniff`，也没有 CSP。

**修复**：
1. `mimeType` 走白名单（只允许图片/pdf/文本等安全类型），未知一律 `application/octet-stream`
2. `Content-Disposition` 改 `attachment`
3. 全局响应头加 `X-Content-Type-Options: nosniff` + `Content-Security-Policy`
4. 文件下载走独立只读域（或至少独立路径），避免与面板同源

---

### P0-2 ｜ users.json 损坏 → 被种子账号静默覆盖，全员账号丢失

**实证确认** ✅

位置：`src/auth.js:62-77` + `src/jsonfile.js:25`

```js
export function loadUsers() {
  try {
    const st = fs.statSync(PATHS.users);        // ① 文件存在，成功
    ...
    const users = readJson(PATHS.users, []);    // ② JSON 损坏 → readJson 抛错
  } catch {
    writeJson(PATHS.users, SEED_USERS);         // ③ 用 alice/bob 种子直接覆盖！
  }
}
```

`jsonfile.js:25` 明确抛 `new Error('状态文件损坏：...')`，直接掉进这个 catch。

**双重后果**：
1. 所有真实账号（含全部同学的 token）被抹掉，全员掉线且**不可恢复**
2. 种子 token `tok_alice_0001` / `tok_bob_0002` **写死在公开源码里**，替换后等于向全网开放管理员权限

实证输出：`损坏前含 carol；损坏后 loadUsers 返回 [0,0] → carol 已丢失`

> 注：`writeJson` 本身是原子的（tmp + rename），正常写入不会损坏。
> 真正的触发场景是磁盘满、强制断电、外部工具/网盘同步篡改 users.json。

**修复**：catch 分支**绝不能写文件**。应改为：抛出明确错误 + 拒绝启动（或进只读保护模式），
并把损坏文件备份为 `users.json.corrupt-<时间戳>` 供人工恢复。

---

### P0-3 ｜ users.json 为空 → 全员无法登录，且无法自愈（死锁）

代码路径确认，同 P0-2 同一函数

```js
const text = fs.readFileSync(file, 'utf8').trim();
if (!text) return structuredClone(fallback);   // 返回 []，不抛错！
```

`readJson` 对空文件返回 `[]` 而非抛错 → `loadUsers` 返回空用户表 →
**所有人 verify 失败 → 全员 401**。而修复所需的 `POST /users` 又要求 `admin:write` 登录态 → **死锁**。

**修复**：空数组同样视为异常，走与 P0-2 相同的保护路径。

---

## P1 —— 功能错误 / 可被利用的缺陷

### P1-1 ｜ 非字符串 title 触发 500（类型混淆）

**实证确认** ✅

| 位置 | `src/context.js:174`、`src/tasks.js:130` |
|------|------------------------------------------|
| 触发 | `POST /context {"title": 123}` 或 `POST /tasks {"title": 456}` |

```js
if (!input.title?.trim()) throw badRequest(...)   // (123).trim is not a function
```

实证：`返回 500 {"code":"INTERNAL","message":"input.title.trim is not a function"}`

同理影响 `description`、`body`、`tags` 等所有只判真假不判类型的字段。
**修复**：入口处统一 `String(input.title ?? '')` 或显式类型校验。

---

### P1-2 ｜ WS 的 agent.status 无频率限制，可灌爆事件日志

**实证确认** ✅

位置：`src/ws.js:128-136`。任何已连接客户端（仅需 `events:read`，**所有 agent 默认拥有**）可无限发送
`agent.status`，每条都 `fs.appendFileSync` 落盘并广播给全部连接。

实证：`50 次写入全部落盘（1 → 51）`

后果：事件日志（唯一真相源）被灌水污染、磁盘写放大、N 个连接 O(N) 广播造成放大攻击。
**修复**：按连接做令牌桶限流，并对 `agent.status` 单独设更严格的配额。

---

### P1-3 ｜ 已完成（done）的任务仍可被他人认领 —— 状态机漏洞

**实证确认** ✅

位置：`src/tasks.js:175-182`，`claim()` 只校验 assignee，**完全不校验 status`。

实证：`done → release → bob claim 返回 200，任务状态=claimed`

后果：已交付的任务被误重新认领，任务板统计与真实进度脱节。
**修复**：`claim()` 开头增加 `if (task.status === 'done') throw conflict('任务已完成')`。

---

### P1-4 ｜ 普通 agent 默认持有 branch:merge，审核闸门形同虚设

**实证确认** ✅

位置：`src/auth.js:33`

```js
const AGENT_SCOPES = SCOPES.filter((s) => s !== 'admin:write');  // 只排除了 admin
```

实证：`默认 scope 含 branch:merge=true，含 admin:write=false`

因为 `review:write` 也在默认清单里，**任意两个普通 agent 配合（或一人开两个身份）即可**：
A 发 PR → B approve → A merge → 代码进 main。README 宣称的"main 受保护，PR 批准 + 可快进才可合"
在默认配置下**不构成任何约束**。

**修复**：从 `AGENT_SCOPES` 移除 `branch:merge`，仅显式授权给受信任成员。

---

### P1-5 ｜ /metrics 无鉴权暴露内部信息

**实证确认** ✅

位置：`src/server.js:174-208`（scope 为 `null`）。无 token 即可读取用户总数、分支数、事件序号、
请求量、WS 连接数。这段代码自己上方的注释写着"无鉴权端点不暴露用户清单"，但 `/metrics` 就暴露了。

**修复**：改为 `AUTH_ONLY` 或至少限制 loopback。

---

### P1-6 ｜ 置顶条目在小 limit 下被裁掉

**实证确认** ✅

位置：`src/context.js:316-320`。先按"置顶优先 + 时间正序"排序，再 `sorted.slice(-limit)`
取**末尾** N 条 —— 置顶项排在最前，反而最先被切掉。

实证：`共 12 条，置顶最老的 n0；limit=3 时置顶项不可见`

**修复**：置顶项单独取出优先保证，剩余名额再按时间截取。

---

### P1-7 ｜ 全部 git 操作同步阻塞事件循环（子代理发现，未实证）

位置：`src/git-repo.js:34-41`，一律 `spawnSync`。`exportRepoBundle()` 会打包整个仓库，
仓库一大就**冻结整个 Hub**——所有 HTTP 请求、WS 心跳全部卡死。
README 里 `keepAliveTimeout = 120s` 那段注释其实就是这个病的症状。

**修复**：改 `spawn` 异步 + Promise 化，或把重活挪到 worker 线程。

---

### P1-8 ｜ 自动更新无完整性校验、无回滚（子代理发现，未实证）

位置：`src/update.js:113-225`、`src/server.js:327-347`。
下载的二进制**无 SHA256/签名校验**即自替换；且顺序为「旧进程先退出、新进程后启动」，
新二进制若损坏或 macOS 签名失效，服务直接下线且 `.old` 不会自动还原。

**修复**：内置公钥验签；改为「先拉起新进程 + 探活成功，再退出旧进程」，失败则回滚。

---

## P2 —— 健壮性 / 体验 / 性能

| # | 问题 | 位置 |
|---|------|------|
| P2-1 | `esc()` 不转义单引号，用于 `onclick='${...}'` 属性是通用隐患（当前因 userId 正则锁死字符而不可利用） | `panel.html:634,1663` |
| P2-2 | 评论删除用 `context:write` scope，权限粒度错配（实测未越权，403 正确拒绝） | `server.js:480` |
| P2-3 | `reject()` 未禁自审（`approve()` 有，它没有） | `reviews.js:138` |
| P2-4 | `/tasks/:id/activity` 每次请求全量读 10000 条事件 | `server.js:441` |
| P2-5 | `PORT` / `limit` / `after` 等未校验 NaN，非法值直达下游 | `config.js:65`、`server.js:351` |
| P2-6 | 429 限流响应在 `res.end` 包装之前返回 → 不计入访问日志与 metrics | `server.js:651` |
| P2-7 | WS 帧解析不支持分片/续帧，浏览器大消息会解析失败（子代理发现） | `wire.js` |

---

## 已排除的误报（复核后判定不成立）

| 报告项 | 复核结论 |
|--------|----------|
| **分支名 XSS**（`panel.html:1663` `onclick="viewDiff('${b.name}')"`） | **不可利用**。分支名只能是 `dev/<userId>`，而 userId 被 `^[A-Za-z0-9_-]{2,32}$` 锁死，无法注入引号。`canPush()` 强制 `branch === privateBranch(userId)`。仅作为 P2-1 的通用隐患保留 |
| **评论删除越权** | **不成立**。实证返回 403，`remove()` 正确校验了 `authorId` |
| **files/tasks 的读改写竞态** | **不成立**。相关函数全同步，Node 单线程下不会被打断（README 已声明单进程假设） |

---

## 修复优先级建议

**第一批（今天）**：P0-1 → P0-2 → P0-3 —— 都是"一旦发生就不可逆"的问题

**第二批（本周）**：P1-1 ~ P1-6 —— 均有明确修复点，改动量小

**第三批（排期）**：P1-7、P1-8、P2 全部

**同时建议补测试**：冒烟测试新增四类断言 —— 类型混淆（非字符串/非数组输入）、
权限粒度（低权凭证访问高权端点）、状态机越界（done 任务再 claim）、安全头（nosniff/CSP 存在性）。
这次 10 个 bug 里 9 个属于这四类，而现有 107 项断言一个都没覆盖。

---

_排查工具已留在 `.probe/verify-bugs.mjs`，改完可直接重跑验证（隔离临时目录，不碰真实数据）。_
