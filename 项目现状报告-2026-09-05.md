# CoAgent Hub 项目现状报告

> 生成时间：2026-09-05 16:05　｜　版本：v0.2.0　｜　分支：main

## 1. 项目一句话

给「多人 × 多台电脑 × 多种 AI agent harness」建一间共享工作室：
共享上下文（笔记墙）、任务板、代码仓（私有分支 + 审核闸门）、实时事件流，
不用传文件夹、不丢上下文。**零第三方依赖**（Node ≥18 ESM + 系统 git）。

## 2. 功能完成度

| 模块 | 状态 | 说明 |
|------|------|------|
| 事件日志（唯一真相源） | ✅ | JSONL 追加式，seq 单调，断线按游标补齐 |
| 共享上下文 | ✅ | 人人可读、仅作者可写、撤回留痕 |
| 任务板 | ✅ | 认领/释放，释放他人任务须出示已批准的审核 |
| 代码仓 | ✅ | Hub 托管裸仓，bundle 推送通道，分支权限硬校验 |
| 审核闸门 | ✅ | PR → 他人批准 → 快进合并；分叉拒绝并给出 rebase 指引 |
| 用户管理 | ✅ | 开户/轮换/注销 API，默认无管理员权限，种子口令启动告警 |
| 实时总线（Phase 2 提前） | ✅ | 零依赖 RFC 6455，订阅过滤，断线自动重连补齐，agent.status |
| 定时轮询 | ✅ | `watch --interval 30m --live 1`（实时+定时混合） |
| Agent CLI | ✅ | init/adduser/note/task/claim/pull/push/status/sync/watch |
| 语义检索（Phase 3） | ⬜ | embedding 字段与接口签名已预留 |

**质量**：端到端冒烟 75 项断言全绿（含权限拒绝、推送通道、CLI 全流程、实时总线），多轮稳定。

## 3. 文件清单（24 个文件，全部各司其职）

```
src/                      核心实现（116K）
├── config.js             路径/端口/受保护分支配置
├── errors.js             统一错误码
├── eventlog.js           追加式事件日志 + 订阅挂点
├── jsonfile.js           可变状态原子读写
├── auth.js               用户/凭证/用户管理 API
├── context.js            共享上下文
├── tasks.js              任务板
├── reviews.js            审核闸门
├── git-repo.js           裸仓编排（bundle 收发/diff/快进合并）
├── wire.js               WebSocket 帧编解码（服务端/客户端共用）
├── ws.js                 实时总线服务端
├── server.js             HTTP 路由与中间件
└── sdk/
    ├── client.js         REST SDK（hub.connect 实时接入）
    └── ws-client.js      WebSocket 客户端（自动重连）
scripts/
├── hub.mjs               agent 命令行工具（13 个子命令）
└── smoke.js              端到端冒烟（75 项断言）
experiments/
└── desktop-launcher/     桌面启动器实验（生成器 + 产物 + 记录）
CoAgent-handoff.md        交接文档（新会话续传用）
README.md                 部署与接入文档
```

## 4. 本次清理结果

- **垃圾文件扫描**：`*.log / *.tmp / *.bak / Thumbs.db` 全域扫描 → **0 个**，工作区本来干净。
- **今日早些时候已清**：4 个调试脚本（debug-push / stress-push / repro-reset / ws-debug / bat-verify）、
  用户桌面上的全部启动器文件、项目根的 Start-CoAgentHub.cmd（按你要求全部移除）。
- **git 工作区**：仅剩 `experiments/` 未入库（本次已随报告一并提交）。
- **运行时数据**：`data/`（用户表/裸仓/事件日志）由 .gitignore 排除，不入库。

## 5. 仓库状态

- 本地 3 个提交，前两个（Phase 1 + 安全加固）已成功推送 GitHub（`ohGGBob/coagent-hub`）。
- 本次报告与实验记录已提交到本地 main。
- ⚠ **推送暂缓**：当前本机代理（127.0.0.1）未开，GitHub 连接被拒。代理恢复后执行
  `git push origin main` 即可；另外本地 origin/main 跟踪引用显示 [gone]，推送后自会恢复。

## 6. 风险与待办（按优先级）

1. **多机真实联测**——唯一挡在验收前的硬事项：主机 `npm start`，同学按 README 接入。
2. **种子 token 轮换**——`tok_alice_0001`/`tok_bob_0002` 写死在公开源码里，正式使用前必须
   `rotate`（服务启动时会自动喊话）。
3. **桌面启动器真机验证**——`experiments/desktop-launcher/out/` 下三个 .bat 已过编码/CRLF/路径
   校验，但沙箱无法代跑 cmd.exe，需你双击验证后决定是否发布。
4. 主机常驻部署（pm2/计划任务，自选）。
5. 远期：Phase 3 语义检索。

## 7. 常用命令

```bash
npm start                                  # 启动 Hub（打印局域网地址）
npm run smoke                              # 冒烟自检
npm run hub -- adduser <id> "名字" --token <管理token>   # 开户→出接入卡片
npm run hub -- watch --interval 30m --live 1             # 常驻监听
```
